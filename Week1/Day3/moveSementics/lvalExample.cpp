#include<bits/stdc++.h>
using namespace std;
/* 
    any form of data that is not preserved in a fixed location in memory will disapper at the end of the expression (full expression) where it originated.
*/
int main(){
    // pure rvlaue which will not convert into lvalue
    10; //10 : temporary(rvalue);
    "satyam";
    vector<int> {1,2,3,4};

    // the variable which have name in the left and a valuee in the right then it is a lvalue 
    //all above exaples are rvalues and the below is lvalue;
    int *ptr = &n1;
    return 0;
}