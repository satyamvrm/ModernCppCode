#ifndef CARTYPE_H
#define CARTYPE_H

//while dealing with th categorical data then we have to use enum
//to limit the user to give only limit option we use enum


enum class CarType{
    SEDAN,
    SUV,
    HATCHBACK
};

#endif // CARTYPE_H
